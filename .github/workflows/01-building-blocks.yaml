name: 01-building-blocks
on: push

jobs:
    echo-hello:
        runs-on: ubuntu-latest
        steps:
            - name: Say hello
              run: echo "hellow world" 
    echo-goodbye:
        runs-on: ubuntu-latest
        steps:
            - name: Sucessful step 
              run: |
                echo "I will succed"
                exit 0
            - name: Say goodbye
              run: echo "Goodbye"
                
                
